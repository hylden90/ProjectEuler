version: 2.0
jobs:
 build:
   docker:
     - image: alpine:3.7
   working_directory: ~/ProjectEuler/projectEuler
   steps:
     - run:
         name: The First Step
         command: |
           pwd
           ls -la
           cd
           ls -la
